import {
  getLibraryStrForCurrentLocale
} from '../../../helpers/get-library-str-for-current-locale';
import { module, test } from 'qunit';

module('GetLibraryStrForCurrentLocaleHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getLibraryStrForCurrentLocale(42);
  assert.ok(result);
});
