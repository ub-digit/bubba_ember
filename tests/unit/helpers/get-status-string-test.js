import {
  getStatusString
} from '../../../helpers/get-status-string';
import { module, test } from 'qunit';

module('GetStatusStringHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getStatusString(42);
  assert.ok(result);
});
