import {
  returnSafeString
} from '../../../helpers/return-safe-string';
import { module, test } from 'qunit';

module('ReturnSafeStringHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = returnSafeString(42);
  assert.ok(result);
});
